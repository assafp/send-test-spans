# send-test-spans

## Installation:

```bash
$ git@github.com:assafp/send-test-spans.git
$ cd send-test-spans
$ bundle install
```
## Usage:

```bash
$ EPSAGON_TOKEN=<token> ruby send_test_spans.rb
```

